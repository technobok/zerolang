# nlit.z -- minimal numeric literal in the body walk (library, like ifctl/asgn).
# Isolates a decimal literal in a binop (coerces to the concrete operand type).

nlit: function {n: i64} out i64 is {
    return n + 1
}
