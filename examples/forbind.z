# forbind.z -- C-style for-loop bindings (counter loops)

countup: function {n: i64} out i64 is {
    s: 0
    for i: 0 while i < n loop {
        s = s + i
        i = i + 1
    }
    return s
}

fromvar: function {n: i64} out i64 is {
    for j: n while j < n loop {
    }
    return n
}
