# hoistctl.z -- call-argument hoisting into synth temps (anf)

bump: function {n: i64} out i64 is {
    return n
}

useBump: function {n: i64} out i64 is {
    return (bump n + 1)
}

useTwice: function {n: i64} out i64 is {
    a: (bump n + 1)
    b: (bump a - 1)
    return b
}
