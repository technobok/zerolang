# interp.z -- minimal string interpolation in the body walk (library, like nlit/asgn).
# Isolates an interpolated string literal: it types as String (not StringView) and
# its interpolated sub-expression (the n in the braces) is walked + stamped.

interp: function {n: i64} out String is {
    return "value: \{n}"
}
