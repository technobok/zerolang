# genmath.z - A generic file unit parameterized by type 't'
# This is a library unit with no 'main' — it is instantiated by other units.

t: Any.generic

add: function {a: t b: t} out t is { return a + b }
double: function {v: t} out t is { return v + v }
