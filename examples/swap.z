# swap.z -- Swap keyword and reassignment
# v1 target: swap, reassignment with =

main: function is {
    a: 10
    b: 20
    print "before: a=\{a} b=\{b}"

    a swap b
    print "after swap: a=\{a} b=\{b}"

    # reassignment
    a = a + b
    print "after a = a + b: a=\{a}"
}
