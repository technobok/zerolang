# multimod.z -- Multi-module program
# v1 target: units, imports, forward reference resolution
# This file imports from mathutil.z (in the same directory)

main: function is {
    result: mathutil.square 7
    print "7 squared = \{result}"

    result2: mathutil.cube 3
    print "3 cubed = \{result2}"
}
