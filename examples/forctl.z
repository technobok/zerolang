# forctl.z -- minimal for-while loop (library module, like mathutil/ifctl).
# Isolates the for body-walk: the for scope, a while condition, an empty body.
# No binding, no iterator, no reassignment, no calls.

loopn: function {n: i64} out i64 is {
    for while n < n loop {
    }
    return n
}
