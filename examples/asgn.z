# asgn.z -- minimal local assignment + reassignment (library, like ifctl/forctl).
# Isolates: a local binding (x: n), a reassignment (x = n), atomid reads.

asgn: function {n: i64} out i64 is {
    x: n
    x = n
    return x
}
