# matchctl.z -- match/case narrowing over a union (library, like ifctl)

Shape: union {
    circle: i64
    rect: String
}

pick: function {s: Shape} out i64 is {
    match (
        s
    ) case circle then {
        return s
    } case rect then {
        return 0
    }
}
