# mathutil.z -- Utility module for multimod example
# v1 target: unit as module, function exports

square: function {n: i64} out i64 is {
    return n * n
}

cube: function {n: i64} out i64 is {
    return n * n * n
}
