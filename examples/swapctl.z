# swapctl.z -- minimal swap statement in the body walk (library, like asgn/nlit).
# Isolates `a swap b` over two owned i64 locals: both operands are resolved + stamped.

swapctl: function {x: i64 y: i64} out i64 is {
    a: x
    b: y
    a swap b
    return a
}
