out/
CLAUDE.md
bin/

# scratch the io examples and two corpus probes write when run by hand from the
# repo root. The run cases execute in a temp dir, so these appear only after a
# manual run -- which is how 13 of them were committed by a `git add -A` once.
/z_io_*
/zwrite.txt
/z_wrap_probe.txt
/protoread_probe.dat
/union_fd_probe.dat
