# stray_system_dir.z -- a folder named `system` beside the program. The stdlib's
# `system` finds its subunits (`ranges` drives `3.each`) in the folder beside
# system.z, never in whatever a src root holds under that name.
main: function is {
    for i: 3.each loop print "\{i}"
}
