box: function {items: List u64} out (Option u64) is {
    return (items.get i: 0)
}

main: function is {
    xs: (List u64)
    xs.append from: 7
    match (box items: xs) case some then print "ok" else null
}
