# app -- opened in the editor, so the overlay answers app.z; its subunit folder
# app/ is on disk only, and the overlay and the src dir are one tree to it.
main: function is {
    n: helper.tag
    print "\{n}"
}
