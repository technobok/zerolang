# Sums the two operands and returns the total.
add: function {
    # the left-hand operand, added first
    a: i64
    # the right-hand operand
    b: i64
} out i64 is {
    return a + b
}

main: function is {
    r: add a: 1 b: 2
    print "\{r}"
}
