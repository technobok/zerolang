Point: class {
    x: i64
    y: i64
} as {
    sum: function {:this} out i64 is {
        return this.x + this.y
    }
}

double: function {n: i64} out i64 is {
    return n * 2
}

main: function is {
    p: (Point x: 3 y: 4)
    s: p.sum
    d: double n: s
    print "\{p.x} \{s} \{d}"
}
