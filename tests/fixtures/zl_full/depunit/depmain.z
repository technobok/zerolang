# depmain.z -- the linted file; its dependency `dep` holds the type error, so
# `zl lint --full` must name dep.z and dep.z's line, not this file's.
main: function is {
    print "\{dep.twice n: 2}"
}
