# inner -- host's subunit, which reads host's `base`. Linted as itself, its
# unused local is reported at THIS file's path and line.
twice: function {n: i64} out i64 is {
    unusedLocal: 5
    return (n * 2) + (base - base)
}
