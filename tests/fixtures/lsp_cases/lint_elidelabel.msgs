{"jsonrpc": "2.0", "id": 1, "method": "initialize", "params": {"rootUri": "file://tests/fixtures/lsp_ws/lintelide", "initializationOptions": {"srcDir": "tests/fixtures/lsp_ws/lintelide", "systemDir": "lib/system"}, "capabilities": {"general": {"positionEncodings": ["utf-32"]}}}}
{"jsonrpc": "2.0", "method": "initialized", "params": {}}
{"jsonrpc": "2.0", "method": "textDocument/didOpen", "params": {"textDocument": {"uri": "file://tests/fixtures/lsp_ws/lintelide/elide.z", "version": 1, "languageId": "zerolang", "text": "# L003 fires on each of the three groups `primaryGenericParam` knows: the list\n# family's element label, the map family's key label, and the option/result\n# family's payload label. The table is nineteen literals with no other reader,\n# so a stale entry stops the rule firing and nothing else goes red -- and the\n# thirty-odd real findings all sit in examples/, which no lint gate reaches.\nHolder: class {\n    xs: (ListRef of: String)\n    m: (MapRR key: String value: u64)\n    o: (Option t: String)\n}\n\n# NO L003: the label is elided already, which is what the rule asks for.\nElided: class {\n    ys: (ListRef String)\n}\n\nmain: function is {\n    print \"elide\"\n}\n"}}}
{"jsonrpc": "2.0", "id": 9, "method": "shutdown"}
{"jsonrpc": "2.0", "method": "exit"}
