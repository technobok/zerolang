# width40 -- the same source laid out at 40 columns
main: function is {
    x: recv.method alpha: 1 beta: 2 gamma: 3
    if alphaCondition == betaCondition then print "y"
    match subject case a then print "aa" case b then print "bb"
}
