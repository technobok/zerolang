# glref.z -- a generic function returning a reference into its argument, called
# from another unit (user_generic_borrow_return.z).
same: function as { t: anyval.generic } in { b: (ListVal t: t).borrow } outx (ListVal t: t).borrow is {
    return b
}
