# holder's own hidden subunit, named for the top-level unit next door. Inside
# holder, `util` is this one -- the nearer scope, and the only one holder can
# reach without going through the top level.
tag: function out i64 is {
    return 77
}
