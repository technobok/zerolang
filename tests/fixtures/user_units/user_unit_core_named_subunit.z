# user_unit_core_named_subunit.z -- a dependency's subunit named like a core
# name resolves to itself inside its parent.
main: function is {
    print "coresub.width 2 7 = \{coresub.width a: 2 b: 7}"
}
