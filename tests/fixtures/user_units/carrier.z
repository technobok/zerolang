# a DEPENDENCY unit with a hidden subunit of its own. Its bodies are reached
# only by the demand drain -- the main unit's tree walk never sees them.
wrap: function {n: i64} out i64 is {
    return (impl.square n: n)
}
