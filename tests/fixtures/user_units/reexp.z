# a unit re-exporting a TYPE from its own hidden subunit -- the only way the
# subunit's contents can be reached at all, since a subunit is private to it.
Thing: impl.Thing

make: function {n: i64} out Thing is {
    return (Thing n: n)
}
