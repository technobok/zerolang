# user_unit_net.z -- a program whose `net` is the user unit next door.
main: function is {
    print "net.listen=\{net.listen 79}"
    print "net.pollReadable=\{net.pollReadable 21}"
    s: net.Sock fd: 3
    print "net.Sock.accept=\{s.accept}"
}
