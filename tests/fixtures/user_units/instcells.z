# instcells.z -- a generic unit and an instance of it, named from another unit
# (user_unit_instance_types.z).
cells: unit as {
    t: anyval.generic
    Cell: record {
        val: t
    }
    make: function {v: t} out Cell is {
        return (Cell val: v)
    }
}
ic: (cells t: i64)
