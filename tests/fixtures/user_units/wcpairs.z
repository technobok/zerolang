# wcpairs.z -- a generic record that writes its `create`, constructed from
# another unit (user_written_create_labels.z).
Pair: record {
    a: t
    b: i64
} as {
    t: anyval.generic
    create: function {v: t} out this is {
        return (meta.create a: v b: 40)
    }
}
