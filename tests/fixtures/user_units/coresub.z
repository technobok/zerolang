# coresub has a hidden subunit named like a core type, `intrange`. The subunit
# folder answers first for every name, core names included, so inside coresub
# `intrange` is the subunit.
width: function {a: i64 b: i64} out i64 is {
    return (intrange.span :a :b)
}
