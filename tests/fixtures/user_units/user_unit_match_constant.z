# A COMPILE-TIME `match` OVER ANOTHER UNIT'S ARM CONSTANT folds like one over
# this unit's.
PICK: match (modes.CUR) case fast then { 1 } case slow then { 2 }

main: function is {
    print "\{PICK}"
}
