# a hidden subunit named for a CONVENTION unit. Inside its parent, `io` is
# this -- the nearer scope -- the same rule a user unit named `tcc` gets at the
# top level.
tag: function out i64 is {
    return 77
}
