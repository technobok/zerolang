# user_written_create_labels.z -- another unit's generic record that writes its
# `create` takes that create's parameters, with its type argument written or
# inferred.
main: function is {
    p: wcpairs.Pair t: u64 v: 2
    q: wcpairs.Pair v: 3.u64
    print "\{p.a} \{p.b} \{q.a} \{q.b}"
}
