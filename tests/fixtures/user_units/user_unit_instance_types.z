# user_unit_instance_types.z -- a type another unit's instance declares, named
# `instcells.ic.Cell` in a signature, is that instance's own copy.
show: function {c: instcells.ic.Cell} out i64 is {
    return c.val
}
main: function is {
    print "\{show c: (instcells.ic.make v: 5)}"
}
