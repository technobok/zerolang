# reserved_unit_nested.z -- a nested unit may not define `view` either
# (E0037); `util.view` would read as the marker, not the function.

util: unit {
    view: function {n: i64} out i64 is { return n + 1 }
}

main: function is {
    print "\{util.view n: 3}"
}
