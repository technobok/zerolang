# reserved_unit_nested.z -- a nested unit may not define `view` either: `view` is
# a keyword (E0033), so `util.view` could only ever be the marker.

util: unit {
    view: function {n: i64} out i64 is { return n + 1 }
}

main: function is {
    print "\{util.view n: 3}"
}
