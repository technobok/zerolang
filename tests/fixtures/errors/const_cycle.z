# E0600: two unit constants each defined in terms of the other. The cycle is
# symmetric, so which of the two the diagnostic names follows the demand: the
# reference in `main` pulls A, A pulls B, and B closing back on A is where the
# guard fires.
A: B + 1
B: A + 1
main: function is {
    print "\{A}"
}
