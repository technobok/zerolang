# E0200: the same for the `.lock` spelling, which is what most of the tree
# still writes.
Inner: class {
    n: i64
}
poke: function {c: Inner.borrow} is {
    g: c.take
    print "n=\{g.n}"
}
main: function is {
    i0: Inner n: 1
    poke c: i0
}
