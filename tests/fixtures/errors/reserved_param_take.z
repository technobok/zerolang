# reserved_param_take.z -- a parameter named `take` is refused: `take` is a
# keyword (E0033), in a parameter list as anywhere a name is declared.
f: function {take: i64} out i64 is { return take }

main: function is { print "\{f take: 1}" }
