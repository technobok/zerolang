# `Bytes xs.borrow` locks `xs` exclusively: reading `xs` while the borrowed
# typedef lives is refused, as it is for any `.borrow` binding.
main: function is {
    xs: ListVal u8
    xs.append from: 1
    b: Bytes xs.borrow
    print "\{xs.length}"
    print "\{b.length}"
}
