# E0003: rebinding a name in the SAME block is a redefinition. A nested block
# shadows (see emitc_corpus/local_shadow.z); rebinding in place does not.
main: function is {
    x: 1
    x: 2
    print "\{x}"
}
