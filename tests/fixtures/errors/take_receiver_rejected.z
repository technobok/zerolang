# E0200: the same for `.take` on a receiver.
Pt: record {
    x: i64
} as {
    grab: function {t: this.take} out i64 is {
        return t.x
    }
}
main: function is {
    q: Pt x: 1
    print "\{q.grab}"
}
