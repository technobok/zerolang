# require_interp_library.z -- `library:` must evaluate to a string at compile
# time. An interpolation reads its parts at run time, so it has no value the
# linker line could be built from (E0600).

libname: "quad"

require: unit {
    library: "\{libname}math"
}

main: function is {
    print "unreachable"
}
