# require_interp_library.z -- `library:` names a link library, so it must be a
# plain string literal, not an interpolation (E0038).

libname: "quad"

require: unit {
    library: "\{libname}math"
}

main: function is {
    print "unreachable"
}
