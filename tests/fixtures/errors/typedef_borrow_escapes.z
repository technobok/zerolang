# a borrowed typedef keeps its source locked, so an `out` function cannot
# return it.
mk: function {xs: ListVal u8} out Bytes is {
    return (Bytes xs.borrow)
}

main: function is {
    xs: ListVal u8
    b: mk :xs
    print "\{b.length}"
}
