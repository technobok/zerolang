# The Box rule: a union arm holds a reftype; a bare valtype arm must be
# spelled (Box T). A marked arm and a null arm are exempt.
pt: record { x: i64 }
U: union {
    n: i64
    p: pt
    ok: String
}
main: function is {
    u: (U.ok "x".string)
    u
}
