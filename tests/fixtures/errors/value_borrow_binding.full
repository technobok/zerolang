error[E0200]: 'pt' is a value type: the binding copies it, so `.borrow` has nothing to borrow
 --> tests/fixtures/errors/value_borrow_binding.z:16:8
  16 |     a: p.borrow
     |        ^
     = hint: drop the marker; a `.borrow` parameter is what refers to the caller's value
error[E0200]: 'kd' is a value type: the binding copies it, so `.borrow` has nothing to borrow
 --> tests/fixtures/errors/value_borrow_binding.z:18:8
  18 |     b: k.borrow
     |        ^
     = hint: drop the marker; a `.borrow` parameter is what refers to the caller's value
error[E0200]: 'pt' is a value type: the binding copies it, so `.borrow` has nothing to borrow
 --> tests/fixtures/errors/value_borrow_binding.z:20:8
  20 |     c: bx.p.borrow
     |        ^
     = hint: drop the marker; a `.borrow` parameter is what refers to the caller's value
error[E0200]: 'i64' is a value type: the binding copies it, so `.borrow` has nothing to borrow
 --> tests/fixtures/errors/value_borrow_binding.z:22:8
  22 |     d: n.borrow
     |        ^
     = hint: drop the marker; a `.borrow` parameter is what refers to the caller's value
4 errors found
