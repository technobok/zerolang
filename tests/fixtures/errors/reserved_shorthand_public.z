# reserved_shorthand_public.z -- the `:public` shorthand declares a member named
# `public`, which is reserved exactly as the spelled `public:` item is (E0037).

Bag: class { a: i64 } as {
    :public
}

main: function is {
    print "unreachable"
}
