# reserved_unit_generic.z -- a unit-level function named `generic` is refused:
# `generic` is a keyword (E0033), the bound of a generic parameter.
generic: function {n: i64} out i64 is { return n }

main: function is { print "\{generic n: 1}" }
