# bare_alias_cycle.z -- mutually recursive BARE unit-level aliases. The dotted
# twin is alias_cycle.z; without the report the ring resolved to a forward
# shell and the emitted C carried a parameter of incomplete type.
Alpha: Beta

Beta: Alpha

probe: function {x: Alpha} is {
    x
}

main: function is {
    print "never"
}
