# A local is not in scope before the line that binds it: a reassignment whose
# target names nothing yet is an unknown reference, as a read would be.
Box: class {
    n: i64
}

main: function is {
    x = 3
    x: 1
    b.n = 4
    b: Box n: 1
    print "\{x} \{b.n}"
}
