# fn_as_names_no_member.z -- a function's `as` item reading a member its type
# does not declare, used as a parameter's type.

f: function as { T: i64.bogus } in { val: T } out i64 is { return 1 }

main: function is {
    print "\{f val: 3}"
}
