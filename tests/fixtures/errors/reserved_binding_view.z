# reserved_binding_view.z -- `view` is a keyword, so `view: 5` is no binding: a
# statement cannot begin with it, and the body ends there (E0035).
main: function is {
    view: 5
    print "\{view}"
}
