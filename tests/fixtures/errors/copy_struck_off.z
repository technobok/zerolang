# E0100: `copy: null` in the `as` block strikes the compiler-provided copy off
# the type, the same way `==: null` opts out of synthesized equality.
Pt: record {
    x: i64
    y: i64
} as {
    copy: null
}
main: function is {
    p: Pt x: 1 y: 2
    q: p.copy
    print "q=\{q.x}"
}
