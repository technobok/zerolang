error[E0100]: parameter 'k' defaults to arm 'num', which carries a payload -- a default names an arm, and cannot supply what the arm holds
 --> arm_param_default_carries_payload.z:11:16
  11 | opt: function {k: kind.num} is {
     |                ^
     = hint: default to an arm that carries nothing, or drop the default and pass the value in
1 error found
