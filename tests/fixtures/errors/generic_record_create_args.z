# E0300: a generic record instance's `create` takes the instance's field types,
# not the template's parameter.
Rec: record {
    a: t
    b: i64
} as { t: anyval.generic }

main: function is {
    yes0: true
    r: (Rec t: u64).create a: yes0 b: 2
    print "\{r.b}"
}
