# a generic typedef whose parameter admits what its base's parameter refuses is
# accepted; each instance whose argument the base refuses is refused, at the
# line that names it. List's element is a reference type.
Bag: typedef (List t) as {
    t: anyval.generic
}
main: function is {
    b: Bag t: i64
    print "\{b.length}"
}
