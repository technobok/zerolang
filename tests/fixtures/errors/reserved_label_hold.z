# reserved_label_hold.z -- `hold` is a keyword, so `hold: 1` is no argument label:
# the call's argument list ends at it (E0019).
f: function {n: i64} out i64 is { return n }

main: function is {
    x: (f hold: 1)
    print "\{x}"
}
