# E0100: a `for loop { ... } while <cond>` post-condition must be a bool or a
# 2-arm sum type, the same rule the pre-condition carries.
triple: variant { lo: null  mid: u64  hi: null }

main: function is {
    t: triple.mid 7
    for loop { print "x" } while t
}
