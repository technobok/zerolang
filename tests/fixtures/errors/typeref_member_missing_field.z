# typeref_member_missing_field.z -- a record field typed `i64.bogus` is reported; it
# used to peel silently to `i64`.
pt: record { b: i64.bogus }

main: function is {
    x: (pt b: 4)
    print "\{x.b}"
}
