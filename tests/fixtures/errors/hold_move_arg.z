# E0200: passing a `hold` binding to a taking parameter moves it out.
Inner: class {
    n: i64
}
eat: function {v: Inner.take} is {
    print "ate \{v.n}"
}
main: function is {
    i0: Inner n: 1
    h: i0.hold
    eat v: h
}
