# E0200: a `hold` binding of a valtype does not accept an implicit downgrade at
# a reassignment; the marker is written again to rebind.
point: record { x: i64 y: i64 }

main: function is {
    p: point x: 1 y: 2
    q: point x: 3 y: 4
    h: p.hold
    h = q
    print "\{h.x}"
}
