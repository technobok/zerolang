error[E0200]: a `.borrow` return must borrow from a non-owned parameter -- mark the source `.view` (readonly) or `.borrow` (mutable), e.g. `t: this.view`, so the returned view has a pinned parent
 --> borrow_return_no_lock.z:4:11
   4 |     peek: function {:this} out String.borrow is { return this.item }
     |           ^^^^
1 error found
