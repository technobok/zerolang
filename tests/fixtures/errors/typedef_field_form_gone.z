# a typedef is written with the `typedef` declarator. The field form it replaced
# means nothing now: `typedef` is a keyword, and a keyword never follows a dot.
m: record { val: u64.typedef }
main: function is {
    x: m val: 3
    print "\{x.val}"
}
