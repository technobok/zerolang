# `not` is declared on bool (negation) and on the integers (complement); a
# String has no such member, so the negation of a string is a plain "no
# member" error rather than a truthiness coercion.
main: function is {
    s: "abc".string
    if s.not then print "empty"
}
