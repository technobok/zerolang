# an argument of a call may not move a value the same call already holds a
# reference to: the callee would read freed memory through `a`
f: function {a: String.borrow b: String.take} is {
    print "\{a} \{b}"
}

main: function is {
    src: "hello".string
    f a: src b: src.take
}
