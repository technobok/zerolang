# reserved_shorthand_hold.z -- the `:hold` shorthand declares a member named
# `hold`, which is reserved exactly as the spelled `hold:` item is (E0037).

cfg: record { a: i64 :hold }

main: function is {
    print "unreachable"
}
