# reserved_shorthand_hold.z -- `hold` is a keyword, so the `:hold` shorthand names
# no member (E0033), exactly as a spelled `hold:` item does not.

cfg: record { a: i64 :hold }

main: function is {
    print "unreachable"
}
