# path_suffix_then_field.z -- a marker ends its path: `p.take.a` reads a field past the `take`
# suffix (E0022).
pt: class { a: i64 }

main: function is {
    p: pt a: 3
    q: p.take.a
    print "\{q}"
}
