# E0400: a generic argument its bound refuses, inferred at a call, is reported
# AT that call -- file, line and column -- not as a positionless error.
main: function is {
    n: 2
    bad: "x" + n
    print "\{bad}"
}
