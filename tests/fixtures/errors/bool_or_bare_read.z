# `or` takes an argument, so a bare dotted read of it is a call with the
# argument missing -- refused at the checker, never emitted as a conversion.
main: function is {
    a: true
    y: a.or
    print "\{y}"
}
