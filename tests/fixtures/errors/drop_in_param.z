# drop_in_param.z -- `.drop` is a statement: a parameter typed `Bag.drop` declares
# nothing, and is refused. It used to run as a borrow.
Bag: class { a: i64 }

f: function {h: Bag.drop} out i64 is { return h.a }

main: function is {
    b: (Bag a: 5)
    print "\{f h: b} \{b.a}"
}
