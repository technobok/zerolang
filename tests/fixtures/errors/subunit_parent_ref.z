# a hidden subunit cannot refer to its parent's definitions: it is read only
# because the parent named it, so it is never declared surface.
compute: function out i64 is {
    r: helper.grab
    return r
}

PARENTC: 5
