# public_value_marker.z -- `.public` is no marker on a value: outside its type a
# value reads only the public surface already.
Bag: class { a: i64 count: i64 } as {
    public: unit { :count }
}

main: function is {
    b: (Bag a: 1 count: 3)
    print "\{b.public.count}"
}
