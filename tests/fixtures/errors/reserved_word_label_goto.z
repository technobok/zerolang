# reserved_word_label_goto.z -- `goto` is a reserved word, so `goto: 1` labels no
# argument (E0033).
f: function {n: i64} out i64 is { return n }

main: function is {
    x: (f goto: 1)
    print "\{x}"
}
