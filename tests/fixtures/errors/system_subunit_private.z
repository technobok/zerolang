# the gated numerics live in implementation subunits of `system`; only
# `system` may name them (E0101). User code spells the type `f16` -- core's
# alias, or `system.f16`.
main: function is {
    z: system.halffloat.f16
    print "\{z}"
}
