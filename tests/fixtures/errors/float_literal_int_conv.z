# `floatliteral` declares only the four float conversions, so an integer target
# is not on its surface. The value is known here, so this is settled at compile
# time rather than deferred to a runtime-checked conversion.
main: function is {
    v: 3.5.u8
    print "v=\{v}"
}
