# out_names_arm.z -- `out mode.slow` names an arm, a value of `mode`, not a type;
# it used to type the return as null ("type 'null' has no member 'fast'").
mode: variant { fast: null slow: null }

f: function out mode.slow is { return mode.fast }

main: function is { print "\{f.fast}" }
