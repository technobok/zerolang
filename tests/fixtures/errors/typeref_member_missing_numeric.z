# typeref_member_missing_numeric.z -- `n: i64.bogus` names no member of i64; it used
# to peel silently to `i64`.
f: function {n: i64.bogus} out i64 is { return n }

main: function is { print "\{f n: 3}" }
