# E0039: a unit with no `main` is a library -- there is no root to demand from,
# so there is nothing to compile it from. The program that opens it is what
# gets compiled. `--eager` needs no root and is exempt.
helper: function {n: i64} out i64 is {
    return n + 1
}
