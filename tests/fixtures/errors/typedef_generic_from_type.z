# a generic typedef's unlabelled argument is `from:`, which takes a value;
# `String` is a type. Its type arguments are labelled (`Bag T: String`).
Bag: typedef (List T) as {
    T: AnyRef.generic
}
main: function is {
    b: Bag String
    print "\{b.length}"
}
