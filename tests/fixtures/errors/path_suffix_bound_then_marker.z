# path_suffix_bound_then_marker.z -- `generic` is a bound, the whole suffix: `anyval.generic.view`
# continues past it (E0022).
f: function as { T: anyval.generic.view } in {x: T} out T is { return x }

main: function is { print "\{f x: 1}" }
