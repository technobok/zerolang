# reserved_private_in_public.z -- nor may a `public:` block hold a `private:` one:
# `private` is a keyword (E0033).

public: unit {
    :main
    private: unit { :helper }
}

helper: function out i64 is { return 3 }

main: function is {
    print "\{helper}"
}
