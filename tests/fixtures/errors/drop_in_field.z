# drop_in_field.z -- a field typed `Bag.drop` is refused: `.drop` is a statement.
Bag: class { a: i64 }

Box: class { b: Bag.drop }

main: function is {
    x: (Box b: (Bag a: 4))
    print "\{x.b.a}"
}
