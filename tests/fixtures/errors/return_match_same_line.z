# As return_if_same_line.z, for a `match`: it is not a binop either, so it is
# left over after `return`.
pick: function {c: bool} out i64 is {
    return match c case true then 1 case false then 2
}

main: function is {
    print "\{pick c: true}"
}
