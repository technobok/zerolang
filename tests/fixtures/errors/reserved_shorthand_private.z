# reserved_shorthand_private.z -- `private` is a keyword, so the `:private` shorthand
# names no member (E0033), exactly as a spelled `private:` item does not.

Bag: class { a: i64 } as {
    :private
}

main: function is {
    print "unreachable"
}
