# reserved_shorthand_private.z -- the `:private` shorthand declares a member named
# `private`, which is reserved exactly as the spelled `private:` item is (E0037).

Bag: class { a: i64 } as {
    :private
}

main: function is {
    print "unreachable"
}
