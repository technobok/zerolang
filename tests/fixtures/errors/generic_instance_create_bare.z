# E0300: `.create` with no arguments on an instance is its bare construction,
# and a field without a default is required.
Pair: class {
    a: t
    b: String
} as { t: anyval.generic }

main: function is {
    p: (Pair t: u64).create
    print "\{p.b}"
}
