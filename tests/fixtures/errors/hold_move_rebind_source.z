# E0200: `h = y.hold` is the sanctioned rebinding, but the SOURCE must be
# owned and mutable -- rebinding one readonly cell from another moves out of
# a readonly source.
main: function is {
    x: "abc".string
    y: "zzzz".string
    h: x.hold
    g: y.hold
    h = g.hold
    print "h=\{h}"
}
