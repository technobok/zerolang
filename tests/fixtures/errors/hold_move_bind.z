# E0200: the same move without a marker. The class has no destructor, so the
# bind-move check's destructor gate does not see it; readonly does.
Inner: class {
    n: i64
}
main: function is {
    i0: Inner n: 1
    h: i0.hold
    stolen: h
    print "n=\{stolen.n}"
}
