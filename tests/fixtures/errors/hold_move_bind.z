# E0200: a `.take` out of a readonly binding. The class has no destructor, so
# the move's destructor gate does not see it; readonly does.
Inner: class {
    n: i64
}
main: function is {
    i0: Inner n: 1
    h: i0.hold
    stolen: h.take
    print "n=\{stolen.n}"
}
