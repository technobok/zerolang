# require_unknown_key.z -- a `require:` entry the compiler does not consume
# would be silently ignored, so it is rejected (E0038).

require: unit {
    libary: "quadmath"
}

main: function is {
    print "unreachable"
}
