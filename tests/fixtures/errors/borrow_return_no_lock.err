error[E0200]: a `.borrow` return must borrow from a non-owned parameter -- mark the source `.view` (readonly) or `.borrow`/`.lock` (mutable), e.g. `t: this.view`, so the returned view has a pinned parent
1 error found
