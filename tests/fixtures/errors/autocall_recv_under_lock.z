# E0200: a zero-argument method auto-calls through the dotted path rather than
# as a Call, and locks its receiver just the same -- so a read conflicts with an
# outstanding exclusive borrow.
main: function is {
    s: "echo".string
    with v: s.borrow do {
        s.length
    }
    print "\{s}"
}
