# `b.byteView` pins `b` shared: growing the buffer while the view lives is
# refused, since the append may move the storage the view reads.
main: function is {
    b: Bytes
    b.append from: 65
    bv: b.byteView
    b.append from: 66
    print "\{bv.length}"
}
