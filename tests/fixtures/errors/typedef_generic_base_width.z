# `Bytes` is built from a `ListVal u8` and nothing else: another element width
# is refused, since the construction never converts.
main: function is {
    xs: ListVal u16
    xs.append from: 65
    b: Bytes xs
    print "\{b.length}"
}
