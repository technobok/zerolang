# reserved_unit_function.z -- a unit-level function named `take` is refused:
# `take` is a keyword (E0033), so `u.take` could only ever be the marker.

take: function {n: i64} out i64 is { return n }

main: function is {
    print "unreachable"
}
