# reserved_unit_function.z -- a unit-level function named `take` is rejected
# (E0037): `take` is an ownership marker in member position, so `u.take`
# could never reach it.

take: function {n: i64} out i64 is { return n }

main: function is {
    print "unreachable"
}
