# `outx` is a function clause. A `data` block's `out T` names its element
# type; there is no result to hold a lock, so `outx` has no meaning there.
masks: data { 1 2 4 } outx u8

main: function is {
    print "\{masks.length}"
}
