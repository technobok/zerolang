# a library unit whose `public:` block exposes openCount and nothing else.
# `secret` is on no surface, so only privlib itself may call it.
public: unit { :openCount }

secret: function {n: i64} out i64 is {
    return n * 2
}

openCount: function {n: i64} out i64 is {
    return (secret :n) + 1
}
