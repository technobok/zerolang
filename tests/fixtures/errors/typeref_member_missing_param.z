# typeref_member_missing_param.z -- a parameter's type `Bag.bogus` names no member of
# Bag, and is reported; it used to peel silently to `Bag`.
Bag: class { a: i64 }

f: function {h: Bag.bogus} out i64 is { return h.a }

main: function is {
    b: (Bag a: 5)
    print "\{f h: b}"
}
