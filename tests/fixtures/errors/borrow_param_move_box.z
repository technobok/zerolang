# E0200: boxing a borrowed parameter moves it into the box, which would
# outlive the borrow.
poke: function {s: String.borrow} is {
    b: Box from: s
    print "boxed"
}
main: function is {
    x: "abc".string
    poke s: x
}
