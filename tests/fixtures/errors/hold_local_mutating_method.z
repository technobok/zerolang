# E0200: a `hold` binding is owned but frozen -- it holds a readonly lock on
# itself for its scope, so a mutating method's exclusive receiver lock is
# refused. `.copy` is the way out.
main: function is {
    x: "abc".string
    h: x.hold
    h.append "d"
    print "\{h}"
}
