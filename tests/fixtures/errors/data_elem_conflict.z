# A data block has one element type. Explicit tags that disagree -- with each
# other or with `out T` -- are refused here rather than resolved by whichever
# one the checker happened to read first.
codes: data { 1.u8  2.u32 } out u16

main: function is {
    print "c=\{codes.0}"
}
