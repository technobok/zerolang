# reserved_word_use_flag.z -- a reserved word READ as a name (`x: flag`) is refused
# as one (E0033), as it is where a name is declared; it used to be E0009
# "unexpected characters".
main: function is {
    x: flag
    print "x"
}
