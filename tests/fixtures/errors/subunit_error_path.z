# E0400 in a SUBUNIT: the location names the subunit's file by its root as
# given joined with its path under that root -- a path that opens from where zc
# ran -- never the path under the root alone.
main: function is {
    print "\{inner.twice n: 2}"
}
