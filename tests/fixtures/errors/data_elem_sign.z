# A negative element in an unsigned block. Worse than the overflow case: -1 to
# 255 is a well-defined C conversion, so the C compiler warns about NOTHING and
# the wrong value simply ships.
codes: data { 10 -1 30 } out u8

main: function is {
    print "c=\{codes.index 1.u64}"
}
