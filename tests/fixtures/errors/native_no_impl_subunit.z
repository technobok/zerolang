# an unbacked `is native` in a SUBUNIT. Nothing calls `spirit`. Until the
# declaration check descended into subunits this was invisible to it, which
# would have mattered the moment natives-tbl-guard stopped reading
# lib/system/*/*.z on the compiler's behalf.
main: function is {
    print "\{hidden.value}"
}
