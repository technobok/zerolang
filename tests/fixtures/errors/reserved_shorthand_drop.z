# reserved_shorthand_drop.z -- the `:drop` shorthand declares a member named
# `drop`, which is reserved exactly as the spelled `drop:` item is (E0037).

Bag: class { a: i64 } as {
    :drop
}

main: function is {
    print "unreachable"
}
