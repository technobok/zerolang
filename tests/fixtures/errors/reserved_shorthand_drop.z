# reserved_shorthand_drop.z -- `drop` is a keyword, so the `:drop` shorthand names
# no member (E0033), exactly as a spelled `drop:` item does not.

Bag: class { a: i64 } as {
    :drop
}

main: function is {
    print "unreachable"
}
