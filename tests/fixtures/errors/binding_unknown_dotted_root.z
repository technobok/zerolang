# a local bound to a dotted value whose root names nothing is an unknown
# reference at the binding -- not a bare name handed to the emitter.
main: function is {
    z: ghost.field
    print "\{z}"
}
