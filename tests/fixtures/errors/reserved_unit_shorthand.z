# reserved_unit_shorthand.z -- the `:hold` shorthand in a unit body declares
# a definition named `hold`, reserved as the spelled one is (E0037).

util: unit {
    :hold
}

main: function is {
    print "unreachable"
}
