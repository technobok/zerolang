# reserved_unit_shorthand.z -- `hold` is a keyword, so the `:hold` shorthand in a
# unit body names no definition (E0033), as a spelled one does not.

util: unit {
    :hold
}

main: function is {
    print "unreachable"
}
