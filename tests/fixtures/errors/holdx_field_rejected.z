# E0200: a field cannot carry a return marker either. Unrefused, `.holdx` on a
# field reached the emitter and produced C the compiler rejected.
Src: class {
    n: i64
}
Holder: class {
    slot: Src.holdx
}
main: function is {
    a: Src n: 5
    h: Holder slot: a
    print "\{h.slot.n}"
}
