# public_nested_private.z -- nor may a `public:` block hold a `private:` one.

public: unit {
    :main
    private: unit { :helper }
}

helper: function out i64 is { return 3 }

main: function is {
    print "\{helper}"
}
