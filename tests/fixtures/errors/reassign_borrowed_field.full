error[E0200]: an owned `out this` whose type declares a non-owned field must be `.takex` -- the result holds a reference into a parameter, and only `takex` pins that source for the result's lifetime
 --> reassign_borrowed_field.z:11:13
  11 |     create: function {item: thing.borrow} out this is {
     |             ^^^^^^
error[E0200]: Cannot reassign '.borrow' field 'item' — a non-owned field is set at construction time and immutable
 --> reassign_borrowed_field.z:16:5
  16 |     h.item = x
     |     ^
error[E0200]: Cannot move borrowed variable 'x' by reassignment — borrowed names stay bound to their source for the full scope; copy with '.copy' to keep the source
 --> reassign_borrowed_field.z:16:5
  16 |     h.item = x
     |     ^
3 errors found
