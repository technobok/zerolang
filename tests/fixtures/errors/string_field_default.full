error[E0100]: field 'tag' is declared by a string literal -- a default must be a constant numeric expression, or an arm that carries nothing
 --> string_field_default.z:19:10
  19 |     tag: "counter".string
     |          ^
     = hint: name the type instead (`tag: String`) and pass the text in, or default it to a number
1 error found
