error[E0100]: field 'tag' is declared by a string literal -- a field's default must be a constant numeric expression
 --> string_field_default.z:19:10
  19 |     tag: "counter".string
     |          ^
     = hint: give the field a type (`tag: String`) and let the constructor supply the text, or default it to a number
1 error found
