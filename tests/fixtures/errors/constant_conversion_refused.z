# A LOSSY CONVERSION OF A CONSTANT IS NO CONSTANT: it yields a result to handle,
# not a value. An untyped constant converts as its literal does, so it must fit.
L: 200.u8
D: 1.5.f64
N: 300

A: L.i8
B: D.f32
C: N.u8

main: function is {
    print "\{A} \{B} \{C}"
}
