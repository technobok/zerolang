# E0200: a `hold` binding does not accept an implicit downgrade at a
# reassignment. The marker has to be written again for the move-and-freeze to
# be visible at the assignment.
main: function is {
    x: "abc".string
    y: "zz".string
    h: x.hold
    h = y
    print "\{h}"
}
