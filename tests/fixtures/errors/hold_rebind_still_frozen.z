# E0200: a rebound `hold` binding is still frozen -- the readonly self-lock is
# retaken over the new value, it is not surrendered by the rebinding.
main: function is {
    x: "abc".string
    y: "zz".string
    h: x.hold
    h = y.hold
    h.append "q"
    print "\{h}"
}
