# E0400: a facet bound admits any argument, so a reftype can reach a record
# field only at instantiation; the mono refuses it there.
fl: facet { area: function {t: this.view} out i64 }
flat: record { s: t } as { t: fl.generic }

main: function is {
    f: flat s: "x".string
    f
    print "x"
}
