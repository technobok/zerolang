# a `.borrow` field pins its source SHARED: a field write on the source is
# refused while the holder lives
Bag: class {
    x: i64
}

D: class {
    b: Bag.borrow
}

main: function is {
    bag: Bag x: 7
    d: D b: bag
    bag.x = 9
    print "\{d.b.x}"
}
