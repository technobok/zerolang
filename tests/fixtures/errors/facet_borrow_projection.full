error[E0200]: 'measurable' is a facet, which holds a copy of its source: `.borrow` has nothing to borrow
 --> tests/fixtures/errors/facet_borrow_projection.z:24:19
  24 |     a: measurable p.borrow
     |                   ^
     = hint: drop the marker -- the projection copies either way
error[E0200]: 'measurable' is a facet, which holds a copy of its source: `.borrow` has nothing to borrow
 --> tests/fixtures/errors/facet_borrow_projection.z:25:26
  25 |     b: measurable.create from: p.borrow
     |                          ^
     = hint: drop the marker -- the projection copies either way
error[E0200]: '(sized n: i64)' is a facet, which holds a copy of its source: `.borrow` has nothing to borrow
 --> tests/fixtures/errors/facet_borrow_projection.z:27:23
  27 |     d: (sized n: i64) p.borrow
     |                       ^
     = hint: drop the marker -- the projection copies either way
3 errors found
