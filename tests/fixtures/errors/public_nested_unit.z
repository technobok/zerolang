# public_nested_unit.z -- a `public:` block inside the file unit's `public:`
# block describes nothing: the interface has no interface of its own.

public: unit {
    :main
    public: unit { :helper }
}

helper: function out i64 is { return 3 }

main: function is {
    print "\{helper}"
}
