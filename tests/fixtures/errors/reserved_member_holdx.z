# E0037: `holdx` joins the member-position reserved names, so a type cannot
# declare a member that would shadow the marker.
Thing: class {
    n: i64
} as {
    holdx: function {:this} out i64 is {
        return this.n
    }
}
main: function is {
    t: Thing n: 1
    print "\{t.n}"
}
