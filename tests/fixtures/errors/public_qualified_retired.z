# public_qualified_retired.z -- `public` is no member of a unit: its `public:`
# block IS the surface, read as `host.MAX`.
host: unit {
    public: unit {
        MAX: 10
    }
}

main: function is {
    print "\{host.public.MAX}"
}
