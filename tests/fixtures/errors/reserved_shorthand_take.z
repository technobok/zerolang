# reserved_shorthand_take.z -- `take` is a keyword, so the `:take` shorthand names
# no member (E0033), exactly as a spelled `take:` item does not.

Bag: class { a: i64 :take }

main: function is {
    print "unreachable"
}
