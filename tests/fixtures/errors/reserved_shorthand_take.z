# reserved_shorthand_take.z -- the `:take` shorthand declares a member named
# `take`, which is reserved exactly as the spelled `take:` item is (E0037).

Bag: class { a: i64 :take }

main: function is {
    print "unreachable"
}
