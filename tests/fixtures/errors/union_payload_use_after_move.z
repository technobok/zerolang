# E0200: a tagged-sum arm construction stores its payload OWNED, so a
# bare-name reftype payload transfers ownership exactly like a `.take`
# argument; a later use of the source is refused.
main: function is {
    s: "abc".string
    o: (Option.some s)
    print "\{s}"
}
