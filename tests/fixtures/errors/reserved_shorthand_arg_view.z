# reserved_shorthand_arg_view.z -- `view` is a keyword, so the `:view` shorthand
# names no argument (E0033).
f: function {n: i64} out i64 is { return n }

main: function is {
    x: (f :view)
    print "\{x}"
}
