# a `.borrow` binding pins its source: a move out from under it would leave
# the alias pointing at freed memory
main: function is {
    src: "hello".string
    b: src.borrow
    t: src.take
    print "\{b} \{t}"
}
