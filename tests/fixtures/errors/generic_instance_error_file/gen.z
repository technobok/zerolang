# the template, in a unit of its own: only an instance is refused, and the
# error names this file and line
public: unit {
    :takeOut
    :Cell
}

Cell: class {
    v: t
} as { t: AnyRef.generic }

takeOut: function as { t: AnyRef.generic } in { c: (Cell t: t) } out t is {
    return c.v.take
}
