# path_suffix_then_member.z -- a marker ends its path: `s.borrow.length` continues past the
# `borrow` suffix (E0022).
main: function is {
    s: "abc".string
    n: s.borrow.length
    print "\{n}"
}
