# A template's `as` constant is the type's, read through the type, and not
# through an instance -- in an instantiation as in any type. It was "no member".
Box: class {
    used: u32
} as {
    cap: u32.generic
    K: cap * 2
}

main: function is {
    b: (Box cap: 8) used: 0
    print "\{b.K}"
}
