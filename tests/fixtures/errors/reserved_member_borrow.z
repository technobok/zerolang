# reserved_member_borrow.z -- a variant arm named `borrow` is refused: `borrow` is
# a keyword (E0033).

modes: variant {
    borrow: null
    ok: null
}

main: function is {
    print "unreachable"
}
