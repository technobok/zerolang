# bool's `and` takes a bool: an integer on the right is an argument-type
# mismatch against the declared method, like `1 + true` is for i64's `+`.
main: function is {
    b: true
    x: b and 1
    print "\{x}"
}
