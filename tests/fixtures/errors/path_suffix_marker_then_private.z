# path_suffix_marker_then_private.z -- `private` comes first in a marker: `.take.private` continues past
# the `take` suffix (E0022).
f: function {x: String.take.private} out u64 is { return 0 }

main: function is { print "\{f x: "a".string}" }
