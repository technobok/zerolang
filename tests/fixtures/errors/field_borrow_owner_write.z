# a `.borrow` field pins its source SHARED: the owner may read it, and may not
# write it while the holder lives
C: class {
    s: String.borrow
}

main: function is {
    src: "hello".string
    c: C s: src
    src.append "x"
    print "\{c.s}"
}
