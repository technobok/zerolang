# path_suffix_twice.z -- a path takes at most one ownership marker: `.view.view` is refused
# at the second dot (E0022).
f: function {xs: (ListVal i64).view.view} out u64 is { return xs.length }

main: function is { print "\{f xs: (ListVal i64)}" }
