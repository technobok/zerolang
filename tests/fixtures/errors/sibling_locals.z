# two sibling functions: neither sees the other's locals (E0023 expected).
f: function out i64 is {
    secret: 41
    return secret
}
g: function out i64 is {
    return secret + 1
}
main: function is { print "\{f} \{g}" }
