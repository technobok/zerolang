# E0200: the same move through a taking argument rather than a marker.
eat: function {v: String.take} is {
    print "ate \{v}"
}
main: function is {
    s: "abc".string
    sv: s.stringView
    eat v: s
    print "sv=\{sv}"
}
