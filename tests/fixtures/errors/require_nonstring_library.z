# require_nonstring_library.z -- `library:` names a link library, so whatever
# it evaluates to has to be a string. A number is a perfectly good compile-time
# value and still not one (E0038).
require: unit {
    library: 42
}

main: function is {
    print "unreachable"
}
