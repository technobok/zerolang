# path_suffix_private_bound.z -- only an ownership marker may follow `private.`: `.private.generic`
# is refused (E0022).
f: function {x: String.private.generic} out u64 is { return 0 }

main: function is { print "\{f x: "a".string}" }
