# E0200: a `.take` out of a `view` parameter, on a class with no destructor --
# the shape the move's destructor gate lets through.
Inner: class {
    n: i64
}
peek: function {c: Inner.view} is {
    g: c.take
    print "n=\{g.n}"
}
main: function is {
    i0: Inner n: 1
    peek c: i0
}
