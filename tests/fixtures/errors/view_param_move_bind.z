# E0200: the same move without a marker, on a class with no destructor -- the
# shape the bind-move check's destructor gate lets through.
Inner: class {
    n: i64
}
peek: function {c: Inner.view} is {
    g: c
    print "n=\{g.n}"
}
main: function is {
    i0: Inner n: 1
    peek c: i0
}
