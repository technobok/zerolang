# E0200: the same for `.hold` -- readonly-ness is what needs the lock, and it
# is the axis both markers share.
Pt: record {
    x: i64
} as {
    peek: function {t: this.hold} out i64 is {
        return t.x
    }
}
main: function is {
    q: Pt x: 1
    print "\{q.peek}"
}
