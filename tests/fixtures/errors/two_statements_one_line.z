# A block line ends at `;`, a new line or `}`: a second statement after a
# complete one on the same line is an error at its first token, and was run as
# though a separator stood between them.
main: function is {
    { print "a" } print "b"
}
