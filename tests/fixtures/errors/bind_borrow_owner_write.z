# a `.borrow` binding is a second name for its source: while it lives the
# owner may read the source but not write it
main: function is {
    src: "hello".string
    b: src.borrow
    src.append "x"
    print "\{b}"
}
