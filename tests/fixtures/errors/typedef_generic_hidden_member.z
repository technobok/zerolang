# a generic typedef hides its base's member with `null`; using it through an
# instance is refused.
Bag: typedef (List t) as {
    t: AnyRef.generic
    append: null
}
main: function is {
    b: Bag t: String
    b.append from: "x".string
    print "\{b.length}"
}
