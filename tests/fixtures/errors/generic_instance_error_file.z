# An error only an instance's walk finds is reported where the template was
# written -- the other unit's file and line -- although the instance walks its
# own copy of the body.
main: function is {
    c: gen.Cell t: String v: "abc".string
    print (gen.takeOut :c)
}
