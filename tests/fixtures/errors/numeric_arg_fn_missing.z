# A GENERIC FUNCTION'S NUMERIC PARAMETER IS WRITTEN AT THE CALL. It is never
# inferred from the other arguments, and a call that did not write it reached
# the emitter.
rep: function as { n: u64.generic } in { v: i64 } out i64 is {
    return v * 2
}

main: function is {
    print "\{rep v: 4}"
}
