# a `with` alias pins its source SHARED: the owner may not write the source
# through its own name while the alias lives
main: function is {
    src: "hello".string
    with v: src.borrow do {
        src.append "x"
        print "\{v}"
    }
}
