# E0040: `main` has no caller to return a value to. The emitter writes
# `void z_main(void)` whatever the declaration says, so an `out` clause
# produced C that returned a value from a void function. The exit status is
# set explicitly through `os.exit`.
main: function out u64 is {
    return 3
}
