# E0100: a literal passed to a concrete parameter of a generic function must
# fit that parameter's type, as it must for any call.
narrow: function as { t: anyval.generic } in { v: t n: u8 } out u8 is {
    return n
}

main: function is {
    print "\{narrow t: i64 v: 5 n: 300}"
}
