# reserved_shorthand_borrow.z -- `borrow` is a keyword, so the `:borrow` shorthand
# names no member (E0033), exactly as a spelled `borrow:` item does not.

cfg: record { a: i64 :borrow }

main: function is {
    print "unreachable"
}
