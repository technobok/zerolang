# reserved_shorthand_borrow.z -- the `:borrow` shorthand declares a member named
# `borrow`, which is reserved exactly as the spelled `borrow:` item is (E0037).

cfg: record { a: i64 :borrow }

main: function is {
    print "unreachable"
}
