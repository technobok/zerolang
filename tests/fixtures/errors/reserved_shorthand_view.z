# reserved_shorthand_view.z -- the `:view` shorthand declares a member named
# `view`, which is reserved exactly as the spelled `view:` item is (E0037).

Bag: class { a: i64 :view }

main: function is {
    print "unreachable"
}
