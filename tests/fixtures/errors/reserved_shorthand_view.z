# reserved_shorthand_view.z -- `view` is a keyword, so the `:view` shorthand names
# no member (E0033), exactly as a spelled `view:` item does not.

Bag: class { a: i64 :view }

main: function is {
    print "unreachable"
}
