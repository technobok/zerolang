# `outx` needs a type: a pin is a property of a RESULT, and a function with
# no result has nothing to hold the locks.
Src: class {
    n: i64
}

mk: function {from: Src.borrow} outx is {
    print "\{from.n}"
}

main: function is {
    a: Src n: 1
    mk from: a
}
