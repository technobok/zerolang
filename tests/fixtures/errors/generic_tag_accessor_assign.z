# E0100: a generic sum's tag accessor is read-only on its instances, as on any
# sum.
slot: variant {
    full: t
    empty: null
} as {
    t: anyval.generic
    k: u8.tag
}

main: function is {
    s: (slot t: i64).full 4
    s.k = 3
    print "x"
}
