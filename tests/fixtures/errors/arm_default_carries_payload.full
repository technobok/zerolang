error[E0100]: field 'n' defaults to arm 'count', which carries a payload -- a default names an arm, and cannot supply what the arm holds
 --> arm_default_carries_payload.z:33:5
  33 |     n: Mixed.count
     |     ^
     = hint: default to an arm that carries nothing, or drop the default and let the constructor build the value
error[E0100]: field 's' defaults to arm 'text', which carries a payload -- a default names an arm, and cannot supply what the arm holds
 --> arm_default_carries_payload.z:35:5
  35 |     s: Payload.text
     |     ^
     = hint: default to an arm that carries nothing, or drop the default and let the constructor build the value
error[E0300]: cannot construct 'Bad' from its bare name: missing required arguments 'n' (type: Mixed), 's' (type: Payload)
 --> arm_default_carries_payload.z:41:8
  41 |     b: Bad
     |        ^^^
     = note: a bare type name in value position is a zero-argument 'create' construction
     = hint: provide each listed argument ('Bad name: value ...') or add field defaults
3 errors found
