# out_drop.z -- `.drop` is a statement: `out Bag.drop` declares nothing, and is
# refused at the declaration rather than as a null return at the use.
Bag: class { a: i64 }

f: function out Bag.drop is { return (Bag a: 2) }

main: function is {
    b: f
    print "\{b.a}"
}
