error[E0100]: 'BAD1' names arm 'count', which carries a payload -- naming an arm gives its tag, and cannot supply what the arm holds
 --> arm_alias_carries_payload.z:31:1
  31 | BAD1: Mixed.count
     | ^^^^
     = hint: supply the payload at each use (`count <value>`), or name an arm that carries nothing
error[E0100]: 'BAD2' names arm 'text', which carries a payload -- naming an arm gives its tag, and cannot supply what the arm holds
 --> arm_alias_carries_payload.z:32:1
  32 | BAD2: Payload.text
     | ^^^^
     = hint: supply the payload at each use (`text <value>`), or name an arm that carries nothing
2 errors found
