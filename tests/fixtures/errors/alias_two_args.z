# E0200: one reftype cannot be passed to two arguments of the same call --
# the callee would hold two mutable references to one object.
Item: class { v: i64 }
sink: function {a: Item b: Item} is {
    print "\{a.v} \{b.v}"
}
main: function is {
    r: Item v: 1
    sink a: r b: r
}
