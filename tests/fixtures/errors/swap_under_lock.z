# E0200: swap writes both operands, so each is gated by an outstanding lock.
Item: class { v: i64 }
main: function is {
    a: Item v: 1
    b: Item v: 2
    with h: a.borrow do {
        a swap b
    }
    print "\{a.v} \{b.v}"
}
