# A unit-level alias's numeric argument is read and range-checked like any other.
# It was "Unknown reference 'Buffer'" before the alias resolved at all.
Buffer: class {
    used: u32
} as { cap: u32.generic }
B: (Buffer cap: -1)

main: function is {
    b: B used: 0
    print "\{b.used}"
}
