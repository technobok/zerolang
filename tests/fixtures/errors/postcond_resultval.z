# E0100: a `for loop { ... } while <cond>` post-condition must be bool, the same
# rule the pre-condition carries. A resultval (from a lossy conversion) is a type
# error rather than a struct in the C `while` head.
main: function is {
    big: 999.u64
    for loop { print "x" } while big.u32
}
