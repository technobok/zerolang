# a reassignment whose value is a bare name nothing defines is an unknown
# reference, as at a binding -- it must not reach C as a bare identifier.
Holder: class {
    n: i64
}
main: function is {
    h: Holder n: 1
    h.n = nothere
    x: 1
    x = alsonot
    print "\{h.n} \{x}"
}
