# a user unit declaring a native the compiler supplies no implementation for.
# Nothing calls it: before the table was total this was accepted in silence.
conjure: function {n: i64} out i64 is native

main: function is {
    print "declared but never called"
}
