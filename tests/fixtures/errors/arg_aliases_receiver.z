# the receiver of a mutating method and an argument of the same call may not
# be one value: the callee writes through the receiver while it reads the
# argument (`String.append` is a generic method, the one path that never
# locked its argument)
main: function is {
    src: "hello".string
    src.append src
    print "\{src}"
}
