# a labelled type argument must agree with the one the value makes
Nums: typedef (ListVal t) as {
    t: anyval.generic
}
main: function is {
    v: ListVal i64
    n: Nums v t: u8
    print "\{n.length}"
}
