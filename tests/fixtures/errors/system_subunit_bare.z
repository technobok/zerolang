# a subunit of `system` is not a unit user code can name: `halffloat` is no
# file unit and no member of any scope this unit sees, so the bare spelling
# is an unknown reference (the type is `f16`).
main: function is {
    z: halffloat.f16
    print "\{z}"
}
