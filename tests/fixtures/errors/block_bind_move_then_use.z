# A bare reftype binding `b: m` MOVES `m`; done inside a block, the move must
# survive the block's close so the read after it is refused.
main: function is {
    m: "abc".string
    {
        b: m
        print "\{b.length}"
    }
    print "\{m.length}"
}
