# the unit type_application_qualified.z constructs through
Pair: record {
    a: t
    b: i64
} as { t: anyval.generic }

Cls: class {
    a: t
    b: String
} as { t: anyval.generic }

slot: variant {
    full: t
    empty: null
} as { t: anyval.generic }
