# E0200: `.view` needs identity, exactly as `.lock` does, so it is rejected on a
# valtype parameter rather than silently doing nothing.
point: record {
    x: i64
    y: i64
}
show: function {p: point.view} is {
    print "\{p.x}"
}
main: function is {
    q: point x: 1 y: 2
    show p: q
}
