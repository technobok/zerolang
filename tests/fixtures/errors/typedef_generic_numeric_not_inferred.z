# a numeric generic parameter is never inferred, from a typedef's base value
# as from a generic class's fields
Label: typedef (str to: n) as {
    n: u64.generic
}
main: function is {
    raw: str to: 8
    l: Label raw
    print "\{l.size}"
}
