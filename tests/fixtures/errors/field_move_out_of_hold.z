# A `hold` binding is frozen: moving a field out of it would write to it.
Holder: class {
    p: String
}

keep: function {s: String.take} out u64 is {
    return s.length
}

main: function is {
    g: Holder p: "abc".string
    h: g.hold
    print "\{keep s: h.p}"
}
