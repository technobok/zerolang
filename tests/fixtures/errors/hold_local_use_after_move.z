# E0200: `.hold` moves the source, exactly as `.take` does -- freezing the
# destination does not make the transfer any less of a transfer.
main: function is {
    x: "abc".string
    h: x.hold
    print "x=\{x} h=\{h}"
}
