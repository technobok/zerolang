# E0100: a bare `for while <cond> loop` condition must be a bool or a 2-arm sum
# type. A THREE-arm variant has no single falsy arm to test against.
triple: variant { lo: null  mid: u64  hi: null }

main: function is {
    t: triple.mid 7
    for while t loop { print "x" }
}
