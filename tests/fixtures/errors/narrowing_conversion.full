error[E0100]: 'f64' does not convert to 'i64' without loss, so it cannot stand in for one
 --> narrowing_conversion.z:19:14
  19 |     i64Takes n: f
     |              ^
     = hint: convert it where the loss is visible -- `i64` returns a result: `v.i64.or d: <fallback>`
error[E0100]: 'u64' does not convert to 'u8' without loss, so it cannot stand in for one
 --> narrowing_conversion.z:20:13
  20 |     u8Takes n: big
     |             ^
     = hint: convert it where the loss is visible -- `u8` returns a result: `v.u8.or d: <fallback>`
error[E0100]: 'u64' does not convert to 'u8' without loss, so it cannot stand in for one
 --> narrowing_conversion.z:22:5
  22 |     h.n = big
     |     ^
     = hint: convert it where the loss is visible -- `u8` returns a result: `v.u8.or d: <fallback>`
error[E0100]: 'u64' does not convert to 'u8' without loss, so it cannot stand in for one
 --> narrowing_conversion.z:24:5
  24 |     small = big
     |     ^
     = hint: convert it where the loss is visible -- `u8` returns a result: `v.u8.or d: <fallback>`
4 errors found
