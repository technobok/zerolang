# with_value_unresolved.z -- a `with` value that resolves nowhere. It used to
# reach the emitter and surface as the internal E0500 unhandled-interp
# fallback rather than a name diagnostic.

main: function is {
    with it: ghostIter do for x: it loop {
        print "\{x}"
    }
}
