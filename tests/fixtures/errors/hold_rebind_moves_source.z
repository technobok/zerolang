# E0200: rebinding a `hold` binding moves the new source in, so the source is
# gone afterwards -- the same transfer the initial `.hold` performs.
main: function is {
    x: "abc".string
    y: "zz".string
    h: x.hold
    h = y.hold
    print "y=\{y}"
}
