# A function declares exactly one of `out` and `outx`: they are the same clause
# with and without the pin, not two clauses.
Src: class {
    n: i64
}

mk: function {from: Src.borrow} out i64 outx i64 is {
    return from.n
}

main: function is {
    a: Src n: 1
    print "\{mk from: a}"
}
