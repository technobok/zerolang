# E0100: a valtype holds value data only. A record may not wrap a reftype in
# a typedef -- `Path` and `Bytes` are classes for exactly this reason.
sid: record { val: String.typedef }

main: function is {
    k: sid
    k
    print "x"
}
