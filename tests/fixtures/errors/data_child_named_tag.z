# `tag` is the name resolveData synthesises for the block's own discriminator,
# so an element claiming it would shadow the tag and leave a tagged type
# reading an element as its discriminator.
codes: data { tag: 1  other: 2 }

main: function is {
    print "o=\{codes.other}"
}
