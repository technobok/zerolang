# PR 4 fixture: identifiers, labels, keywords, reserved words,
# operators-as-REFIDs, numeric-shaped REFIDs with dot continuation.
# No string literals (those land in PR 5).

foo bar baz
foo: bar
:this
function f is { x + y }
match (m) case a then { z } else { w }
record r is { x: 0 y: 0 }
flag enum macro
1.5 + a.b
+42 -7
out in is as with on
