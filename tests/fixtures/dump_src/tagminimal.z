# tagminimal.z -- the smallest unit that renders an arm value in the canonical
# dump: a variant NOTHING references but the one arm constant. That shape is
# what makes the constant read mint the type itself, and it is the allocation
# pattern that exposed a canon renderer freeing a string-pool buffer.
oskind: variant {
    linux: null
    darwin: null
}

plt: oskind.linux
