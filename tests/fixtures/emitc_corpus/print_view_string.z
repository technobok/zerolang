# `print` of a `String.view` parameter: the print only reads the string, so a
# const view reaches it without discarding the qualifier.
say: function {s: String.view} is {
    print s
}
main: function is {
    say s: "alpha".string
    t: "beta".string
    say s: t
}
