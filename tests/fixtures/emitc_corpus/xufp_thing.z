# xufp_thing -- the unit on the far side of xunit_create_fieldpath: a class whose
# user `create` takes a class from a third unit
public: unit { :Thing }
Thing: class { v: u64 } as {
    create: function {pool: zast.StringPool} out this is {
        return (meta.create v: pool.length)
    }
}
