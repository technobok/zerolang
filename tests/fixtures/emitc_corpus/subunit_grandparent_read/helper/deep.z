# a subunit of a subunit: its parent's HELPC, its grandparent's GRANDC and
# core's optionval, all reached by the ordinary outward walk.
mul: function {n: i64} out i64 is {
    o: optionval.some (n * GRANDC + HELPC)
    match o case some then return o
    return 0
}
