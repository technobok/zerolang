# sibling field paths never overlap, so one call may lock both
Inner: class { n: i64 }
Outer: class {
    a: Inner
    b: Inner
}
sink: function {p: Inner q: Inner} is {
    print "\{p.n} \{q.n}"
}
main: function is {
    o: (Outer a: (Inner n: 1) b: (Inner n: 2))
    sink p: o.a q: o.b
}
