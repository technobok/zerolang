# order_call_dep.z -- the functions order_call_args reaches in another unit.
tick: function {n: i64} out i64 is {
    print "t\{n}"
    return n
}

pair: function {a: i64 b: i64} out i64 is {
    return (a * 10) + b
}
