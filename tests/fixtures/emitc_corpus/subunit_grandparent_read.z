# subunit_grandparent_read.z -- a hidden subunit two levels down resolves
# outward through every enclosing unit: `deep` reads helper's constant, the
# file unit's constant, and core's `optionval`, all by bare name.
GRANDC: 100

main: function is {
    print "helper.chain 3 = \{helper.chain 3}"
}
