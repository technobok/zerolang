# a unit whose generic types are constructed through its name
# (type_application_values.z)
Pair: record {
    a: t
    b: i64
} as { t: anyval.generic }

Cls: class {
    a: t
    b: String
} as { t: anyval.generic }
