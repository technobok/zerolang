# A dependency unit whose data block has the SAME name as the main unit's. Its
# own values must be read, and its array must be emitted alongside main's.
tab: data { 100 101 102 103 } out u8
lookup: function {i: u64} out u8 is { return (tab.index i) }
