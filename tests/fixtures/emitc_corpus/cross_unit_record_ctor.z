Box: union {
    lit: xunit_rec.LitData
}

mk: function {v: u64} out Box is {
    return (Box.lit (xunit_rec.LitData val: v))
}

main: function is {
    b: mk v: 7.u64
    match (
        b
    ) case lit then {
        print "lit"
    }
}
