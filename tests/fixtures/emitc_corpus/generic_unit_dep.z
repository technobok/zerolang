# generic_unit_dep.z -- exercises dep_generic, which declares and instantiates a
# generic unit of its own rather than in the main unit.
main: function is {
    print "dep_generic.sum 3 5 = \{dep_generic.sum a: 3 b: 5}"
}
