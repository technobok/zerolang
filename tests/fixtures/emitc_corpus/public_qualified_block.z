# public_qualified_block.z -- the block's contents ARE the surface, spelled
# `pubinl_host.MAX` from outside; `public` names no member of the unit (inside
# it, `public.MAX` reaches the block by its own name). Pinned.
main: function is {
    print "\{pubinl_host.MAX} \{pubinl_host.twice}"
}
