# nested-receiver calls lock their full receiver path; with nothing else
# holding a lock they compose freely, one call scope at a time
Bag: class { items: (ListVal i64) }
main: function is {
    b: Bag items: (ListVal i64)
    b.items.append 3
    b.items.append 4
    print "\{b.items.length}"
}
