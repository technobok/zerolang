# unit_level_bare_type.z -- a bare type name at unit level is an ALIAS, the
# same as the dotted form. The alias then works everywhere its target does:
# in value position it is the zero-value construction, like `v: u64` itself.
x: u64

main: function is {
    print "\{x}"
}
