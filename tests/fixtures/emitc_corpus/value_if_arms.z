# AN `if` USED AS A VALUE MAY RUN A BLOCK IN AN ARM, whose last statement is the
# arm's value, and yield an owned String.
main: function is {
    c: 3 > 2
    v: if c then {
        a: 2
        a * 3
    } else { 4 }
    s: if c then { "a".string } else { "b".string }
    print "\{v} \{s}"
}
