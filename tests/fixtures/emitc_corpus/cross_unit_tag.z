main: function is {
    c: xunit_tag.color.GREEN
    r: xunit_tag.isRed c: c
    if r then {
        print "red"
    } else {
        print "notred"
    }
    if c == xunit_tag.color.GREEN then {
        print "green"
    } else {
        print "notgreen"
    }
}
