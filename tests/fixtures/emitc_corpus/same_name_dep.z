# A Cell of its own, for same_name_types_across_units: the same name as the
# main unit's and a nested unit's, a third layout and a third method body.
Cell: class {
    v: i64
} as {
    get: function {c: this} out i64 is {
        return c.v * 10
    }
}
