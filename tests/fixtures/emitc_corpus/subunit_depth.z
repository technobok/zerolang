# subunit_depth.z -- a subunit may have subunits of its own, and a name written
# inside one resolves against IT first: `deep` below is helper's subunit, not a
# sibling of helper. The parser already loaded to any depth; resolution is what
# was capped.
main: function is {
    print "helper.chain 6 = \{helper.chain 6}"
}
