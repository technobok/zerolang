svlen: function {sv: StringView} out u64 is {
    return (sv.length)
}

useName: function {nm: String} out u64 is {
    return (svlen sv: nm.stringview)
}

main: function is {
    s: "hello".string
    print "\{useName nm: s}"
}
