# a part with a written `==`, for emitc_corpus/eq_part_own_eq: declared in a
# unit of its own
rpoint: record {
    x: i64
    y: i64
} as {
    ==: function {a: this rhs: rpoint} out bool is {
        return a.x == rhs.x
    }
}
