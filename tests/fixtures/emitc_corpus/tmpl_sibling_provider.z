# tmpl_sibling_provider.z -- a template whose body reads a sibling of ITS OWN
# unit (OFFSET); the instantiating unit defines no OFFSET.
OFFSET: 100

shifter: unit as {
    t: anyval.generic
    shift: function {v: t} out t is { return v + OFFSET }
}
