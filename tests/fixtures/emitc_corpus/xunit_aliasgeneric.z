# xunit_aliasgeneric -- the unit next door whose GENERIC template a renaming
# re-export names. Nothing here is a stdlib type, so nothing about it can be
# answered by a container special-case.
Widget: class {
    val: t
} as { t: anyval.generic }
