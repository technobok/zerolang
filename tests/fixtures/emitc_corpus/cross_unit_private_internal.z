# the public half of xunit_privlib: reaching a unit's public definition, which
# reaches that unit's PRIVATE one internally. errors/private_cross_unit.z pins
# the other direction.
main: function is {
    print "\{xunit_privlib.openCount n: 5}"
}
