# A FUNCTION'S LAST EXPRESSION IS ITS RESULT: a bare name and a string literal
# are returned, widening where the declared type is wider.
widen: function {x: u8} out i64 is {
    x
}

label: function out StringView is {
    "done"
}

main: function is {
    print "\{widen x: 7} \{label}"
}
