# ifaceuse -- consumes ifacelib through its public interface: `areaOf` and the
# renamed `perimeter`. `ifacelib.perimImpl` / `ifacelib.secret` would be E0101.
main: function is {
    a: ifacelib.areaOf w: 3 h: 4
    p: ifacelib.perimeter w: 3 h: 4
    print "area=\{a} perim=\{p}"
}
