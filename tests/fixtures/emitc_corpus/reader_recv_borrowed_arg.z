# a reader receiver takes a shared lock, so an argument borrowing the same
# container composes with it
main: function is {
    xs: (List String)
    xs.append "a".string
    print "\{xs.contains item: (xs.get i: 0)}"
}
