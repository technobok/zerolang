# a hidden subunit of data_nested_unit: a data block read from inside the
# subunit and through its name from the parent.
codes: data {
    ht: 9
    lf: 10
} out u8

pick: function {i: u64} out u8 is {
    return (codes.index i)
}
